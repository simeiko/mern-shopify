import React from 'react';

/**
 * Displays "Loading..." screen.
 */
export default class Loading extends React.Component {
    render() {
        return (
            <div id="loading">
                <p>Loading...</p>
            </div>
        );
    }
}